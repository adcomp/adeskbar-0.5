0.5.2	- updates:
		- fix battery applet (but make it depend on python-power)
		- add option for hiding quit from context menu
		- add fix for popups so their text color can be fixed through
		  gtk-2.0 theme's gtkrc if text in popups and tooltips is
		  unreadable.
		- update systray applet. Add some options for manipulating
		  applet's background when bar is set to use gradient bg.
		- add images for battery applet.

0.5.0	- new 'expander' plugin
		- new system for plugins
		- fix for dual screen (testing)
		* fix 'places' plugin : thanks FSnow
		* new plugins : thanks Vala
			- mail / alarm / weather

0.4.3   - fix bug in "tasklist"
        - tasklist now follow icon size
        - adeskbar should refresh on "gtk icon theme" change
        - menu(s) should auto-update when add/remove applications

0.4.2   - plugin "drawer" is come back
        - some changes to plugin "tasklist"
        - improvement for "grandient" ( ui )

0.4.1a  - fix bug : crash when no "settings" menu .. 

0.4.1   - fix "menu" plugin bug ( freeze because Thread )
        - fix dependencies ( python-xdg ) in DEB package
        
0.4     - new plugin 'menu (simple)', Window list.
        - interactive config ( don't need to refresh anymore )
        
